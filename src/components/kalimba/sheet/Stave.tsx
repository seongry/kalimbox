/** @jsx jsx */

import { stave } from "@/components/kalimba/sheet/styles";
import { jsx } from "@emotion/react";
import { FC } from "react";

export const Stave: FC = () => (
    <div css={stave}>디테일은 기능 구현하면서 추가 예정</div>
);
